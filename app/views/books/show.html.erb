<br>
<br>
<br>
<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-xs-3">
   <h2>User info</h2>
   <table class='table'>
    <tr><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%></tr> 
    <tr>
      <th>name</th>
      <th><%= @user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= @user.introduction %></th>
    </tr>
  </table>
  <p><%= link_to '', edit_user_path(current_user.id),class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench " %></p>
  <br>

  <h2>New book</h2>

  <%= form_for(@book_new, url: '/books') do |f| %>
  <% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited
    this book from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |msg|%>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <p>Title</p>
  <%= f.text_field :title, class: "field" %>


</p>Opinion</p>
<%= f.text_area :body,class: "field" %><br>

<%= f.submit 'Create Book', class: "btn btn-primary col-xs-12" %>

<% end %>
</div>
<div class="col-xs-9">
  <h1>Book detail</h1>
  <table class='table' >

    <tr>
      <th><%= link_to user_path(@book.user.id) do %>
        <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg",size: "60x60"%>
          
        <%= @user.name %><% end %></th>

      <td><%= @book.title %></td>
      <td><%= @book.body %></td>

      <div>
        <% if current_user.id == @user.id %>

        <td><%= link_to 'Edit', edit_book_path(@book), class: 'btn-sm btn-success' %></td>
        <td><%= link_to 'Destroy', book_path(@book), method: :delete, class: "btn-sm btn-danger destroy_book_27368" %></td>

        <%  end %>
      </div>


    </tr>
  </table>
</div>
</div>